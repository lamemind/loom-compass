# Registry legacy per-macchina — contiene path di lavoro e UUID di profili
# Ptyxis locali: NON versionabile (questo repo è pubblico).
# Modello di riferimento: compass@lamemind/projects.example.json
compass@lamemind/projects.json
